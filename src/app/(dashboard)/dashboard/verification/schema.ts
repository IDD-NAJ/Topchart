// Structured data schema component for the verification page
// Renders a JSON-LD script tag for SEO purposes

export function VerificationServiceSchema(): null {
  // Server-side structured data is injected via layout metadata.
  // This client-side component intentionally returns null.
  return null;
}
